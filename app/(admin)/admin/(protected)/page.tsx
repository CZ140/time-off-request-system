// app/(admin)/admin/(protected)/page.tsx
// URL: /admin — inside (protected) layout for auth enforcement.
// Full dashboard content replaced in Plan 02.
export default function AdminDashboardPage() {
  return <main><p>Admin dashboard — coming in Phase 4</p></main>
}
