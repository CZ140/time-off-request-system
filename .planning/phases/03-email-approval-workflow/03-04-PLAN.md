---
phase: 03-email-approval-workflow
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/reviewed/page.tsx
  - app/invalid/page.tsx
autonomous: true
requirements:
  - APPR-01
  - APPR-02

must_haves:
  truths:
    - "Admin landing on /reviewed sees the request status, teacher name, dates, leave type, and which admin reviewed it"
    - "Admin landing on /invalid sees a clear error message indicating the link is invalid — no request details shown"
    - "Both pages are server components that read searchParams asynchronously (Next.js 15 requirement)"
    - "Both pages follow the same Tailwind styling conventions as other public pages in the project"
  artifacts:
    - path: "app/reviewed/page.tsx"
      provides: "Already-reviewed informational page"
      contains: "searchParams"
    - path: "app/invalid/page.tsx"
      provides: "Invalid/missing token error page"
      contains: "invalid"
  key_links:
    - from: "app/api/approve/route.ts"
      to: "app/reviewed/page.tsx"
      via: "NextResponse.redirect(new URL('/reviewed?...', request.url))"
      pattern: "/reviewed"
    - from: "app/api/approve/route.ts"
      to: "app/invalid/page.tsx"
      via: "NextResponse.redirect(new URL('/invalid', request.url))"
      pattern: "/invalid"
---

<objective>
Create the /reviewed and /invalid pages that the approval route handler redirects to after processing an approval link click.

Purpose: Without these pages, the route handler's redirects 404. Admins clicking Approve or Deny land on these pages — they must be clear and informative. The "already reviewed" page is distinct from the error page per locked decisions.

Output: app/reviewed/page.tsx, app/invalid/page.tsx
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-email-approval-workflow/03-CONTEXT.md

<interfaces>
<!-- Query params each page receives from /api/approve redirects. -->

/reviewed receives these query params (all string, URL-encoded):
  status      — "approved" | "denied"
  teacher_name
  start_date  — ISO date string e.g. "2026-04-15"
  end_date    — ISO date string
  leave_type  — LeaveType union value e.g. "sick"
  reviewed_by — admin email that took the action

/invalid receives no query params — it is a static error page.

Next.js 15 searchParams pattern (established in existing confirmation page):
```typescript
type Props = {
  searchParams: Promise<{ [key: string]: string | undefined }>
}
export default async function PageName({ searchParams }: Props) {
  const params = await searchParams
  // params.status, params.teacher_name, etc.
}
```

Date formatting for display — replicate the same local-time parsing from email templates:
```typescript
function formatDate(iso: string): string {
  return new Date(iso + 'T00:00:00').toLocaleDateString('en-US', {
    month: 'long', day: 'numeric', year: 'numeric',
  })
}
```
(Define inline in reviewed/page.tsx — no shared import needed for a page component)

Leave type labels map (define inline in reviewed/page.tsx):
```typescript
const LEAVE_TYPE_LABELS: Record<string, string> = {
  sick: 'Sick Leave', personal: 'Personal Leave', vacation: 'Vacation',
  bereavement: 'Bereavement Leave', jury_duty: 'Jury Duty',
  professional_development: 'Professional Development',
  maternity_paternity: 'Maternity / Paternity Leave',
}
```

Tailwind styling conventions (from existing public pages — plain Tailwind 4, no component library):
- Page container: min-h-screen bg-gray-50 flex items-center justify-center p-6
- Card: bg-white rounded-lg shadow-sm border border-gray-200 p-8 max-w-lg w-full
- Status badge (approved): bg-green-100 text-green-800 text-sm font-medium px-3 py-1 rounded-full
- Status badge (denied): bg-red-100 text-red-800 text-sm font-medium px-3 py-1 rounded-full
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /reviewed page</name>
  <files>app/reviewed/page.tsx</files>
  <action>
Create app/reviewed/page.tsx as a Next.js 15 async server component.

Locked decisions (from CONTEXT.md):
- New dedicated route — not reusing /confirmation
- Shows: current status (Approved / Denied as a badge), teacher name, dates, leave type, and which admin reviewed it
- This page handles BOTH cases: successful action redirect AND second-click idempotency redirect

Page content:
- Heading: "Request Already Reviewed" (suitable for both outcomes — the admin may have been the one who took the action, or a second admin arriving after the first)
- Status badge showing "Approved" or "Denied" based on status param (green for approved, red for denied)
- Details section showing: Teacher Name, Leave Type (use LEAVE_TYPE_LABELS map), Start Date (formatted), End Date (formatted), Reviewed By (the admin email)
- Brief message below heading: "This leave request has already been reviewed. No further action is needed."

Handle edge case: if any required param is missing/undefined, show a graceful fallback (e.g., "—") rather than crashing. The page should always render.

Use async/await pattern for searchParams (Next.js 15 requirement).

Styling: follow Tailwind conventions from interfaces above. This is Claude's discretion per CONTEXT.md — use clean, professional layout consistent with existing public pages.
  </action>
  <verify>
    <automated>cd "/c/Users/Chris/Documents/Coding projects/time off request system" && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>app/reviewed/page.tsx exists, uses async searchParams, displays status badge and request details, lint passes</done>
</task>

<task type="auto">
  <name>Task 2: Create /invalid page</name>
  <files>app/invalid/page.tsx</files>
  <action>
Create app/invalid/page.tsx as a Next.js 15 server component (static — no searchParams needed).

Locked decisions (from CONTEXT.md):
- Separate from /reviewed — invalid token is a different failure mode from already-reviewed
- Copy is Claude's discretion — clear, not alarming

Page content:
- Heading: "Invalid Approval Link"
- Brief explanation: something like "This approval link is invalid, has expired, or has already been used. If you received this link in an email, it may have been corrupted or the request no longer exists."
- No request details (there are none — the token failed validation before any DB fetch)
- No call to action beyond the implicit "close this tab"

Styling: same Tailwind conventions as /reviewed but use a neutral/amber color scheme for the icon/badge area rather than green/red (this is an error state, not an approval outcome). Clean card layout.

No searchParams, no async props needed — this is a simple static page.
  </action>
  <verify>
    <automated>cd "/c/Users/Chris/Documents/Coding projects/time off request system" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>app/invalid/page.tsx exists as a static server component. npm run build passes for both new pages.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- npm run build passes
- Both pages render without crashing when navigated to directly in the browser
- /reviewed with sample params shows status badge, teacher details, reviewed_by
- /invalid shows the error message without any params required
</verification>

<success_criteria>
Both pages exist and are type-correct. The approval route handler can redirect to them without 404s. Build is green.
</success_criteria>

<output>
After completion, create .planning/phases/03-email-approval-workflow/03-04-SUMMARY.md
</output>
