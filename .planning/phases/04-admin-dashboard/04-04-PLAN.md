---
phase: 04-admin-dashboard
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-05
  - ADMIN-06
  - ADMIN-07
  - ADMIN-08

must_haves:
  truths:
    - "All 8 ADMIN requirements pass manual smoke test"
    - "npm run build exits 0 with zero TypeScript or lint errors"
    - "CVE-2025-29927 bypass attempt via curl header still redirects to login"
  artifacts:
    - path: "app/(admin)/admin/(protected)/page.tsx"
      provides: "Verified working dashboard page"
    - path: "app/(admin)/admin/_components/RequestsTab.tsx"
      provides: "Verified filter and sort functionality"
    - path: "app/(admin)/admin/_components/BlackoutDatesTab.tsx"
      provides: "Verified CRUD with inline confirm"
  key_links:
    - from: "curl bypass test"
      to: "layout.tsx second check"
      via: "x-middleware-subrequest header skips middleware; layout must still redirect"
      pattern: "getSession"
---

<objective>
Run the full build, then execute the manual smoke test checklist to verify all 8 ADMIN requirements are working end-to-end.

Purpose: This is the Phase 4 gate. All prior automated checks (lint + build) passed per-task. This checkpoint confirms the complete user-facing flows work correctly in a running dev server.
Output: Human confirmation that the admin dashboard is ready for Phase 5.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-admin-dashboard/04-VALIDATION.md
@.planning/phases/04-admin-dashboard/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build gate</name>
  <files></files>
  <action>
Run the full build and lint suite. Fix any remaining TypeScript errors or lint warnings before proceeding to the human verify checkpoint.

Commands to run in sequence:
1. `npm run lint` — must exit 0
2. `npm run build` — must exit 0 with no errors

If either fails: diagnose the error, fix the affected file, re-run until both pass. Do not proceed to the checkpoint task until the build is clean.

Common issues to check if build fails:
- Missing `'use client'` on components that use hooks (`useState`, `useActionState`, `useRouter`)
- `app/(admin)/admin/(protected)/page.tsx` importing from `../actions` — verify `actions.ts` exports match
- `BlackoutDatesTab.tsx` importing `formatDate` from `@/lib/email/utils` — confirm the import path is correct
- Stub `BlackoutDatesTab.tsx` still in place if Plan 03 didn't fully replace it
</action>
  <verify>
    <automated>cd "/c/Users/Chris/Documents/Coding projects/time off request system" && npm run lint && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>npm run lint exits 0; npm run build exits 0; zero TypeScript errors in output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 4 admin dashboard:
    - Login page at /admin/login (centered card, inline error on wrong password)
    - Protected dashboard at /admin (requests table + blackout dates tab)
    - CVE-2025-29927 dual-auth: middleware + layout both check session
    - Requests tab: 10 columns, status filter pills, sortable column headers
    - Blackout Dates tab: inline add form, inline confirm delete, empty state
    - Logout button clears cookie and redirects to /admin/login
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev`

Run through this checklist in order. Each item must pass before marking approved.

**Auth flow (ADMIN-01, ADMIN-02):**
1. Navigate to http://localhost:3000/admin — should redirect to /admin/login
2. Submit login form with wrong password — should show "Incorrect password. Please try again." inline with no redirect
3. Submit login form with correct ADMIN_PASSWORD from .env.local — should redirect to /admin dashboard
4. CVE bypass test (requires curl): `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/admin -H "x-middleware-subrequest: pages-router-edge-server/next-edge-server"` — should return 307 (redirect), NOT 200
5. Click Logout — should redirect to /admin/login; re-visiting /admin should redirect to login again

**Requests tab (ADMIN-03, ADMIN-04, ADMIN-05):**
6. Navigate to /admin after logging in — Requests tab is default
7. Verify all 10 columns are present: Teacher Name, Email, Leave Type, Start Date, End Date, Reason, Blackout?, Status, Submitted, Reviewed By
8. Verify status badges are color-coded: Pending=yellow, Approved=green, Denied=red, Auto-Denied=gray
9. Click the "Pending" filter pill — only pending rows should appear; click "All" to restore
10. Click any column header — rows should re-sort; click same header again — direction should toggle (↑/↓ indicator visible)

**Blackout Dates tab (ADMIN-06, ADMIN-07, ADMIN-08):**
11. Click "Blackout Dates" tab — should show existing ranges (or "No blackout dates set." if empty)
12. Fill in the inline add form (Label + Start Date + End Date) and click Add — new range should appear in the list and form fields should clear
13. Click Delete on any row — button should change to "Confirm?" and "Cancel"
14. Click Cancel — row should remain, buttons should return to single Delete
15. Click Delete again, then click "Confirm?" — row should be removed from the list
  </how-to-verify>
  <resume-signal>Type "approved" if all 15 checks pass. Describe any failures so they can be fixed before Phase 5.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when:
- [ ] `npm run build` exits 0
- [ ] All 15 smoke test items pass
- [ ] Human types "approved"
</verification>

<success_criteria>
- Build is clean: zero lint errors, zero TypeScript errors
- All 8 ADMIN requirements verified working in running dev server
- CVE-2025-29927 dual-auth confirmed: bypass header returns redirect, not 200
- Phase 4 gate passed — ready for `/gsd:verify-work` and Phase 5
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-04-SUMMARY.md`
</output>
