---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - .env.local
  - .env.example
  - app/layout.tsx
  - app/(public)/page.tsx
  - app/(public)/confirmation/page.tsx
  - app/(admin)/admin/page.tsx
  - app/(admin)/admin/login/page.tsx
  - app/api/approve/route.ts
  - middleware.ts
  - tailwind.config.ts
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "Next.js 15 app boots locally with `npm run dev` and no errors"
    - "All secret env vars are documented in .env.example without NEXT_PUBLIC_ prefix"
    - "Directory structure matches the locked layout from CONTEXT.md exactly"
    - "`npm run build` completes with zero TypeScript or compilation errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies with next@^15 pinned"
      contains: '"next": "^15'
    - path: ".env.example"
      provides: "Env var documentation reference for SEC-02 audit"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "app/layout.tsx"
      provides: "Root layout — App Router entry point"
    - path: "app/(public)/page.tsx"
      provides: "Teacher form route placeholder"
    - path: "app/(admin)/admin/page.tsx"
      provides: "Admin dashboard route placeholder"
    - path: "middleware.ts"
      provides: "Next.js 15 middleware stub (used in Phase 4 for admin auth)"
  key_links:
    - from: ".env.example"
      to: ".env.local"
      via: "documentation — developer copies example to local"
      pattern: "SESSION_SECRET|SUPABASE_SERVICE_ROLE_KEY|APPROVAL_SECRET|ADMIN_PASSWORD"
---

<objective>
Bootstrap the Next.js 15 App Router project with the exact directory structure, dependencies, and environment variable conventions that every later phase builds on.

Purpose: Establish the project foundation — correct framework version, dependency set, folder layout, and environment variable security posture documented before any feature code is written.
Output: A bootable Next.js 15 app with placeholder routes, all prod dependencies installed, `.env.example` documenting all required secrets without `NEXT_PUBLIC_` prefix (SEC-02), and a `middleware.ts` stub ready for Phase 4.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 app and install dependencies</name>
  <files>package.json, next.config.ts, tsconfig.json, tailwind.config.ts</files>
  <action>
    Run the following in the project root (the directory already exists — do NOT create a subdirectory):

    ```bash
    npx create-next-app@15 . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*" --yes
    ```

    CRITICAL: Use `create-next-app@15`, NOT `@latest`. As of March 2026, `@latest` installs Next.js 16 which has breaking changes (proxy.ts instead of middleware.ts, Turbopack default, removed APIs). The `@15` tag pins to the latest 15.x.

    After scaffolding, install remaining production dependencies:

    ```bash
    npm install @supabase/supabase-js iron-session resend server-only
    npm install --save-dev @types/node
    ```

    Verify `package.json` shows `"next": "^15` (not `^16`). If create-next-app installed 16 despite the `@15` tag, manually update the version: `npm install next@^15.5.12`.

    Delete the default `app/page.tsx` placeholder content and the boilerplate in `app/globals.css` (keep the Tailwind directives: `@tailwind base; @tailwind components; @tailwind utilities;`).

    Do not modify `next.config.ts` or `tsconfig.json` beyond what create-next-app generates — the defaults are correct for this project.
  </action>
  <verify>
    <automated>cd "C:/Users/Chris/Documents/Coding projects/time off request system" && node -e "const p=require('./package.json'); const v=p.dependencies.next; if(!v.startsWith('^15') && !v.startsWith('15')) { process.exit(1); } console.log('next version ok:', v);"</automated>
  </verify>
  <done>package.json exists with next@^15.x, @supabase/supabase-js, iron-session, resend, server-only installed. `npm run dev` starts without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create directory structure and placeholder route files</name>
  <files>
    app/(public)/page.tsx,
    app/(public)/confirmation/page.tsx,
    app/(admin)/admin/page.tsx,
    app/(admin)/admin/_components/.gitkeep,
    app/(admin)/admin/login/page.tsx,
    app/api/approve/route.ts,
    components/ui/.gitkeep,
    lib/supabase/.gitkeep,
    lib/email/.gitkeep,
    lib/auth/.gitkeep,
    types/.gitkeep,
    middleware.ts
  </files>
  <action>
    Create every directory and placeholder file from the locked layout in CONTEXT.md. Exact structure required:

    ```
    app/
    ├── (public)/
    │   ├── page.tsx              ← teacher form (placeholder)
    │   └── confirmation/
    │       └── page.tsx          ← confirmation page (placeholder)
    ├── (admin)/
    │   ├── admin/
    │   │   ├── page.tsx          ← dashboard (placeholder)
    │   │   └── _components/      ← admin-specific components (empty dir)
    │   └── admin/login/
    │       └── page.tsx          ← admin login (placeholder)
    ├── api/
    │   └── approve/
    │       └── route.ts          ← approval handler (placeholder)
    └── layout.tsx                ← already created by create-next-app
    components/
    └── ui/                       ← shared reusable UI (empty dir)
    lib/
    ├── supabase/                 ← populated in Plan 03
    ├── email/                    ← populated in Plan 03
    └── auth/                     ← populated in Plan 03
    types/                        ← populated in Plan 02
    ```

    Placeholder page content for all `page.tsx` files (replace create-next-app defaults if any):

    ```tsx
    // app/(public)/page.tsx
    export default function TeacherFormPage() {
      return <main><p>Teacher form — coming in Phase 2</p></main>
    }
    ```

    ```tsx
    // app/(public)/confirmation/page.tsx
    export default function ConfirmationPage() {
      return <main><p>Confirmation — coming in Phase 2</p></main>
    }
    ```

    ```tsx
    // app/(admin)/admin/page.tsx
    export default function AdminDashboardPage() {
      return <main><p>Admin dashboard — coming in Phase 4</p></main>
    }
    ```

    ```tsx
    // app/(admin)/admin/login/page.tsx
    export default function AdminLoginPage() {
      return <main><p>Admin login — coming in Phase 4</p></main>
    }
    ```

    ```ts
    // app/api/approve/route.ts
    import { NextResponse } from 'next/server'

    export async function GET() {
      return NextResponse.json({ message: 'Approval handler — coming in Phase 3' })
    }
    ```

    ```ts
    // middleware.ts — root level (NOT inside app/)
    import { NextResponse } from 'next/server'
    import type { NextRequest } from 'next/server'

    export function middleware(request: NextRequest) {
      // Admin auth enforcement added in Phase 4
      return NextResponse.next()
    }

    export const config = {
      matcher: ['/admin/:path*'],
    }
    ```

    Use `.gitkeep` files (empty) for `components/ui/`, `lib/supabase/`, `lib/email/`, `lib/auth/`, and `types/` to preserve the empty directories in git. These directories are populated in Plans 02 and 03.
  </action>
  <verify>
    <automated>cd "C:/Users/Chris/Documents/Coding projects/time off request system" && node -e "const fs=require('fs'); const required=['app/(public)/page.tsx','app/(public)/confirmation/page.tsx','app/(admin)/admin/page.tsx','app/(admin)/admin/login/page.tsx','app/api/approve/route.ts','middleware.ts']; const missing=required.filter(f=>!fs.existsSync(f)); if(missing.length){console.error('Missing:',missing);process.exit(1);} console.log('All route files present');"</automated>
  </verify>
  <done>All directories and placeholder files exist. `npm run build` compiles without errors (all placeholder routes are valid TypeScript).</done>
</task>

<task type="auto">
  <name>Task 3: Create .env.example and .env.local with SEC-02-compliant variable names</name>
  <files>.env.example, .env.local, .gitignore</files>
  <action>
    Create `.env.example` documenting every env var the system requires. This file IS committed to git — it contains no values, only names and descriptions:

    ```bash
    # .env.example
    # Copy this file to .env.local and fill in the values.
    # NEVER commit .env.local — it is in .gitignore.
    #
    # SEC-02 ENFORCED: No secret variable uses the NEXT_PUBLIC_ prefix.
    # NEXT_PUBLIC_ variables are embedded in the browser bundle by Next.js.
    # All variables below are server-only by default.

    # ── Supabase ──────────────────────────────────────────────────────────────
    # Project URL from: Supabase Dashboard → Project Settings → API
    SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

    # Service role key from: Supabase Dashboard → Project Settings → API
    # WARNING: This key bypasses Row Level Security. Server-side only.
    SUPABASE_SERVICE_ROLE_KEY=eyJ...

    # ── Resend (Email) ────────────────────────────────────────────────────────
    # API key from: resend.com → API Keys
    RESEND_API_KEY=re_...

    # Admin email address(es) that receive leave request notifications.
    # Comma-separated for multiple recipients: admin@school.edu,principal@school.edu
    ADMIN_EMAILS=admin@school.edu

    # The "from" address for outbound emails. Must be a verified Resend domain.
    RESEND_FROM=Time Off System <noreply@yourdomain.com>

    # ── Approval Workflow ────────────────────────────────────────────────────
    # Shared secret appended to approve/deny URLs for token validation (APPR-01).
    # Generate with: openssl rand -base64 32
    APPROVAL_SECRET=

    # ── Admin Dashboard ──────────────────────────────────────────────────────
    # Password for the admin dashboard login page (ADMIN-01).
    ADMIN_PASSWORD=

    # Secret for encrypting the admin session cookie (iron-session).
    # MUST be at least 32 characters. Generate with: openssl rand -base64 32
    SESSION_SECRET=
    ```

    Create `.env.local` as a working copy. Leave secret values empty — the user will fill these in. The file should mirror `.env.example` structure but is NOT committed:

    ```bash
    # .env.local — DO NOT COMMIT
    SUPABASE_URL=
    SUPABASE_SERVICE_ROLE_KEY=
    RESEND_API_KEY=
    ADMIN_EMAILS=
    RESEND_FROM=Time Off System <noreply@yourdomain.com>
    APPROVAL_SECRET=
    ADMIN_PASSWORD=
    SESSION_SECRET=
    ```

    Verify `.gitignore` (created by create-next-app) already contains `.env.local`. If it does not, add it. The `.env.example` file must NOT be in `.gitignore` — it is safe to commit.

    SEC-02 audit: Confirm no variable name in either file begins with `NEXT_PUBLIC_`. Every secret listed uses a plain name (no prefix). This is the mechanism Next.js uses to keep vars server-only.
  </action>
  <verify>
    <automated>cd "C:/Users/Chris/Documents/Coding projects/time off request system" && grep -r "NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY\|NEXT_PUBLIC_APPROVAL_SECRET\|NEXT_PUBLIC_ADMIN_PASSWORD\|NEXT_PUBLIC_SESSION_SECRET" .env.example .env.local 2>/dev/null && echo "VIOLATION: NEXT_PUBLIC_ found on secret" && exit 1 || echo "SEC-02 pass: no NEXT_PUBLIC_ on secrets"</automated>
  </verify>
  <done>`.env.example` committed. `.env.local` exists locally with empty values. No secret variable uses `NEXT_PUBLIC_` prefix. `.env.local` is in `.gitignore`.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run:

```bash
cd "C:/Users/Chris/Documents/Coding projects/time off request system"
npm run build
```

Build must complete with zero errors. This is the phase-level gate.

Also verify the Next.js version is 15.x:
```bash
npx next --version
```

Output must show `15.x.x`.
</verification>

<success_criteria>
1. `package.json` shows `"next": "^15.x.x"` and all production deps installed: `@supabase/supabase-js`, `iron-session`, `resend`, `server-only`
2. All placeholder routes exist: `app/(public)/page.tsx`, `app/(public)/confirmation/page.tsx`, `app/(admin)/admin/page.tsx`, `app/(admin)/admin/login/page.tsx`, `app/api/approve/route.ts`
3. `middleware.ts` exists at project root (not inside `app/`)
4. `.env.example` is committed with all 8 env var names, none prefixed with `NEXT_PUBLIC_` (SEC-02)
5. `.env.local` exists locally, is in `.gitignore`, is not committed
6. `npm run build` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
